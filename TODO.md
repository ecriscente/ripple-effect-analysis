# The Zeitgeist Engine - TODO

## Backend (Phase 1)

- [x] Create a new module (e.g., `llm_integration.py`) to handle all interactions with the Large Language Model API.
- [x] Securely manage the LLM API key using environment variables (e.g., using `python-dotenv`).
- [x] Replace the hardcoded sample response in `main.py` with a real call to the LLM, using the three master prompts from our framework.
- [x] Implement error handling for the LLM API call (e.g., if the API is down or returns an error).

## Frontend (Phase 1)

- [x] Refine the UI to better handle and display potential errors from the backend.
- [x] Add a "Copy to Clipboard" button for each section of the analysis.
- [x] Improve the visual distinction between the different sections of the report.

## General

- [x] Create a `README.md` for the project with setup and running instructions.